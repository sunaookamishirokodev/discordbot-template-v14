const ExtendedClient = require('../../classes/ExtendedClient');

module.exports = {
    event: '',
    once: false,
    /**
     * 
     * @param {ExtendedClient} client
     * @param {*} args... 
     * @returns 
     */
    run: (client, args...) => {
        
    }
};